var actionTypes = require('./constants');
var actions = require( './actions' );

var addBookmarkReducer = function( state, action ) {
  state = state || {};
  switch (action.type) {
    case actionTypes.ADD_BOOKMARK_SUCCESS:
        newbookmarkArray = state.bookmark.
    default: 
      return state;
  };
};

